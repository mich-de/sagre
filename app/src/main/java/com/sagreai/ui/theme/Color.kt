package com.sagreai.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Colors ─────────────────────────────────────────────────────────────
// Palette ispirata ai colori caldi delle sagre italiane: luci, tramonto, estate

val OrangeHot = Color(0xFFFF6B35)       // Arancio fuoco principale
val OrangeWarm = Color(0xFFFF8C42)      // Arancio caldo secondario
val OrangeLight = Color(0xFFFFB347)     // Arancio chiaro accent

val PurpleDeep = Color(0xFF7B2D8B)      // Viola profondo (sagra notturna)
val PurpleMid = Color(0xFF9C4DB8)       // Viola medio
val PurpleLight = Color(0xFFBD7FE0)     // Viola chiaro

val GoldPrimary = Color(0xFFFFD700)     // Oro (stelle, luci festa)
val GoldDark = Color(0xFFB8860B)        // Oro scuro

// ── Dark Theme ────────────────────────────────────────────────────────────────
val BackgroundDark = Color(0xFF0D0D1A)  // Quasi nero con sfumatura blu notturno
val SurfaceDark = Color(0xFF1A1A2E)     // Card background scuro
val Surface2Dark = Color(0xFF16213E)    // Surface variante
val Surface3Dark = Color(0xFF0F3460)    // Surface deep blue

val OnBackgroundDark = Color(0xFFF0EBF8) // Testo principale (leggermente viola)
val OnSurfaceDark = Color(0xFFE8E0F0)    // Testo su card
val OnSurfaceVariantDark = Color(0xFFB0A8C4) // Testo secondario
val OutlineDark = Color(0xFF4A4060)      // Bordi

// ── Light Theme ───────────────────────────────────────────────────────────────
val BackgroundLight = Color(0xFFFFF8F0)  // Crema calda
val SurfaceLight = Color(0xFFFFFFFF)
val Surface2Light = Color(0xFFFFF3E8)
val OnBackgroundLight = Color(0xFF1A0A00)
val OnSurfaceLight = Color(0xFF2D1200)
val OnSurfaceVariantLight = Color(0xFF6B4226)
val OutlineLight = Color(0xFFD4956A)

// ── Semantic ──────────────────────────────────────────────────────────────────
val SuccessGreen = Color(0xFF4CAF50)
val ErrorRed = Color(0xFFE53935)
val WarningAmber = Color(0xFFFFB300)
val InfoBlue = Color(0xFF2196F3)
